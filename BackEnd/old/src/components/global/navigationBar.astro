---
import Logo from "@global/logo.astro";

const Tabs = [
  { name: "Tea", link: "/teaList" },
  { name: "Benefits", link: "/benefits" },
  { name: "Recipes", link: "/recipes" },
  { name: "Profile", link: "/profile" },
];
---

<nav class="fixed w-full px-6 z-50 mt-4">
  <div class="w-full h-8 flex flex-row items-center justify-between mb-2 gap-8">
    <div
      class="left-0 h-16 flex items-center content-center overflow-hidden mb-2"
    >
      <Logo />
    </div>

    <div class="flex flex-col h-full w-full items-center">
      <div class="flex flex-row gap-16 items-center h-full" id="tabs">
        {
          Tabs.map((data) => (
            <a class="text-center" href={data.link}>
              <p class="text-white Text leading-none">{data.name}</p>
            </a>
          ))
        }
      </div>
      <hr class="border-b-px  border-white w-full px-auto" />
    </div>
    <div
      id="modes"
      class="border w-8 h-8 rounded-full flex items-center justify-center"
      role="button"
    >
      <img src="/svg/Sun.svg" alt="Logo" class="w-6 block dark:hidden" />
      <img src="/svg/Moon.svg" alt="Logo" class="w-5 hidden dark:block" />
    </div>
  </div>

  <div
    class="fixed w-full h-48 top-0 left-0 from-[#000A] bg-gradient-to-b -z-10"
  >
  </div>
</nav>

<script>
  function toggleDarkMode() {
    document.documentElement.classList.toggle("dark");
    const isDark = document.documentElement.classList.contains("dark");
    localStorage.setItem("theme", isDark ? "dark" : "light");
    console.log(localStorage.getItem("theme"));
  }

  if (typeof window !== "undefined") {
    const savedTheme = localStorage.getItem("theme");
    if (savedTheme) {
      document.documentElement.classList.toggle("dark", savedTheme === "dark");
    }
  }

  // const modeButton = document.getElementById("modes") as HTMLInputElement;

  // modeButton.addEventListener("click", toggleDarkMode);
</script>
